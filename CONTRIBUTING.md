# Contributing

欢迎提交修复、教学说明、公开数据适配器和不依赖付费 API 的基线。

1. 从 `main` 创建分支；
2. 保持每个课程文件可以独立运行；
3. 新功能需要添加测试；
4. 不提交 API 密钥、真实个人航迹或大型数据文件；
5. 提交前运行 `python -m pytest -q` 和 `ruff check .`。

涉及公开无人机数据集时，请在 PR 中注明数据来源、许可证、坐标系、单位和采样率。

